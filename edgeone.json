{
  "name": "sub2api-edgeone",
  "framework": "vite",
  "buildCommand": "cd frontend && pnpm install && pnpm build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "directory": "functions"
  },
  "cloudFunctions": {
    "overseasRegions": ["ap-singapore"]
  }
}
